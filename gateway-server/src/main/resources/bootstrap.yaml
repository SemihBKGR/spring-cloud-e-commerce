spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    config:
      uri: http://${config.server.address:localhost}:${config.server.port:8888}
      username: user
      password: pass