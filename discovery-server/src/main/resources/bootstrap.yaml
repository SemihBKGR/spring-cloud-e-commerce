server:
  port: 8761
spring:
  application:
    name: discovery
  cloud:
    config:
      uri: http://${config.server.address:localhost}:${config.server.port:8888}
      username: user
      password: pass